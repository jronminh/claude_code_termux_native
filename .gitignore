# Never accidentally commit Anthropic's proprietary binary or any local
# install artifacts if someone copies/tests inside a clone of this repo.
claude
claude.prev
claude.rejected
manifest.json
manifest.json.prev
*.lock
*.log
.repatch-history
